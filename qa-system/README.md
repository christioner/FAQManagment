# FAQ Management System

智能问答管理系统 - 基于 Vue3 + Spring Boot 的现代化FAQ管理平台

## 🎯 项目概述

本系统是一个功能完善的FAQ(常见问题)管理系统,支持问答的增删改查、分类管理、文档上传以及AI自动生成问答等功能。

### 主要特性

- ✅ **现代化UI设计** - 基于Element Plus的响应式界面,支持深色主题
- ✅ **智能问答管理** - 完整的CRUD操作,支持分类、搜索、统计
- ✅ **增强型Markdown编辑器** - 支持实时预览、分屏编辑、语法高亮
- ✅ **用户认证系统** - 登录/注册功能,基于JWT的安全认证
- ✅ **分类管理** - 灵活的分类体系
- ✅ **文档上传** - 支持PDF、Word、TXT等格式(准备集成AI)
- 🔄 **AI集成** - 基于GLM(智谱AI)的自动问答生成(开发中)

## 🏗️ 技术栈

### 后端
- **框架**: Spring Boot 3.1.5
- **语言**: Java 17
- **数据库**: MySQL 8.0
- **ORM**: Spring Data JPA
- **安全**: Spring Security
- **构建工具**: Maven

### 前端
- **框架**: Vue 3.3
- **构建工具**: Vite 4.4
- **UI库**: Element Plus 2.4
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **HTTP客户端**: Axios

### 基础设施
- **容器化**: Docker + Docker Compose
- **文件存储**: MinIO
- **缓存**: Redis (可选)

## 📁 项目结构

```
FAQManagment/
├── qa-system-backend/          # Spring Boot后端
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/
│   │   │   │   └── com/qa/system/
│   │   │   │       ├── entity/       # 数据实体
│   │   │   │       ├── repository/   # 数据访问层
│   │   │   │       ├── service/      # 业务逻辑层
│   │   │   │       ├── controller/   # REST API控制器
│   │   │   │       ├── config/       # 配置类
│   │   │   │       ├── dto/          # 数据传输对象
│   │   │   │       └── common/       # 公共类
│   │   │   └── resources/
│   │   │       └── application.yml   # 应用配置
│   │   └── test/
│   ├── pom.xml                        # Maven配置
│   └── Dockerfile
│
├── qa-system-frontend/         # Vue3前端
│   ├── src/
│   │   ├── api/                # API接口定义
│   │   ├── assets/             # 静态资源
│   │   ├── components/         # 可复用组件
│   │   ├── router/             # 路由配置
│   │   ├── store/              # Pinia状态管理
│   │   ├── utils/              # 工具函数
│   │   ├── views/              # 页面组件
│   │   │   ├── Login.vue
│   │   │   ├── Dashboard.vue
│   │   │   ├── qa/
│   │   │   ├── category/
│   │   │   └── document/
│   │   ├── App.vue
│   │   └── main.js
│   ├── package.json
│   ├── vite.config.js
│   └── Dockerfile
│
└── docker-compose.yml          # Docker编排配置
```

## 🚀 快速开始

### 前置要求

- Java 17+
- Node.js 18+
- MySQL 8.0+
- Maven 3.6+

### 数据库准备

1. 创建数据库:
```sql
CREATE DATABASE faqmanagment CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. 数据库连接信息:
- Host: 172.19.10.40:33306
- Database: faqmanagment
- Username: root
- Password: report_TaySEM

> 注: 表结构会在应用启动时自动创建(JPA auto-ddl)

### 后端启动

```bash
# 1. 进入后端目录
cd qa-system-backend

# 2. 构建项目
mvn clean package -DskipTests

# 3. 运行应用
java -jar target/qa-system-backend-1.0.0.jar

# 后端将在 http://localhost:8080 启动
```

### 前端启动

```bash
# 1. 进入前端目录
cd qa-system-frontend

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 前端将在 http://localhost:3000 启动
```

## 🎨 功能展示

### 1. 登录界面
- 现代化渐变设计
- 登录/注册切换
- 表单验证

### 2. 问答管理
- 列表展示(分页、搜索)
- 创建/编辑问答
- **增强型Markdown编辑器**:
  - 实时预览
  - 分屏编辑
  - 工具栏快捷按钮
  - 语法高亮
  - 支持标题、加粗、斜体、代码、列表、链接等

### 3. 分类管理
- 分类CRUD操作
- 对话框编辑

### 4. 文档管理
- 文档上传(UI已完成)
- AI处理(待实现)

## 📖 API文档

### 认证接口

```
POST   /api/auth/login          # 用户登录
POST   /api/auth/register       # 用户注册
```

### 问答管理

```
GET    /api/qa                  # 分页查询问答列表
GET    /api/qa/{id}             # 获取问答详情
POST   /api/qa                  # 创建问答
PUT    /api/qa/{id}             # 更新问答
DELETE /api/qa/{id}             # 删除问答
POST   /api/qa/{id}/like        # 点赞
```

### 分类管理

```
GET    /api/categories          # 获取分类列表
POST   /api/categories          # 创建分类
PUT    /api/categories/{id}     # 更新分类
DELETE /api/categories/{id}     # 删除分类
```

### 健康检查

```
GET    /api/health              # 健康检查
```

## 🔧 配置说明

### 后端配置 (application.yml)

```yaml
spring:
  datasource:
    url: jdbc:mysql://172.19.10.40:33306/faqmanagment
    username: root
    password: report_TaySEM

# AI配置 (GLM)
ai:
  openai:
    api-key: your-glm-api-key
    base-url: https://open.bigmodel.cn/api/paas/v4/
    model: glm-4
```

### 前端配置 (vite.config.js)

```javascript
server: {
  port: 3000,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true
    }
  }
}
```

## 🐳 Docker部署

```bash
# 启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 停止所有服务
docker-compose down
```

服务端口:
- 前端: http://localhost:80
- 后端: http://localhost:8080
- MinIO控制台: http://localhost:9001

## 📝 开发计划

### 已完成 ✅
- [x] 基础项目结构
- [x] 用户认证系统
- [x] 问答CRUD功能
- [x] 分类管理
- [x] 增强型Markdown编辑器
- [x] 数据库集成
- [x] 前后端联调

### 进行中 🔄
- [ ] AI集成(GLM)
- [ ] 文档上传与解析
- [ ] MinIO文件存储

### 计划中 📅
- [ ] 完整JWT认证
- [ ] 角色权限管理
- [ ] 搜索优化
- [ ] 数据导入导出
- [ ] 单元测试
- [ ] API文档(Swagger)

## 🤝 贡献指南

欢迎提交Issue和Pull Request来帮助改进项目。

## 📄 许可证

MIT License

## 👨‍💻 作者

开发者 - FAQ Management System

---

**当前状态**: ✅ 核心功能已完成,系统可正常运行
